services:
  api:
    build: .
    ports: ["8000:8000"]  
    environment:
      - PORT=8000
      - DATABASE_URL=postgres://postgres:dev@db:5432/tasks
    depends_on:
      db:
        condition: service_healthy  # Veritabanı tamamen hazır olana kadar bekle
  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: dev
      POSTGRES_DB: tasks
    ports:
      - "5432:5432" 
    volumes:
      - taskdata:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d tasks"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  taskdata: